const rules = [
    'if the number is divisible by 3 write "foo" instead of the number',
    'if the number is divisible by 5 add "bar"',
    'if the number is divisible by 7 add "qix"',
    'for each digit 3,5,7 add "foo", "bar","qix" in the digit order'
]

module.exports = {
    rules
}